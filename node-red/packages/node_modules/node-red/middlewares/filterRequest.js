const filterBodyRequest = (req, res, next) => {
  console.log(req.user);
  if (req.body) {
    if (req.body.password)
      req.body.password = require('bcryptjs').hashSync(req.body.password, 8);
  }
  next();
};

module.exports = {
  filterBodyRequest,
};
